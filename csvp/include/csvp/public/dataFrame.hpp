#pragma once

namespace csvp {
    // CSV DataFrame Model
    class DataFrame
    {
    public:
        // Constructors
        DataFrame();

        // Destructor
        ~DataFrame();

        // Getters

    private:
        // Attributes
        int _rows; // rows count
        int _cols; // columns count

        // cells array

        // columns array

        // rows array
    };
}